meta {
  name: 1. Create Child
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/child
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "firstName": "Emma",
    "lastName": "Wilson",
    "birthDate": "2018-03-10"
  }
}

script:post-response {
  if (res.getStatus() === 201) {
      var jsonData = res.getBody();
      bru.setVar("testChildId", jsonData.id);
  }
}

settings {
  encodeUrl: true
}
