meta {
  name: Delete Child
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/child/{{childId}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  Delete a child and all their gifts
}
